# 持仓复检功能实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在趋势扫描功能基础上增加持仓跟踪和复检机制，用户可标记已买入的品种，系统重新打分并给出持有/减仓/平仓建议

**Architecture:** 新增 usePositions composable 管理 localStorage 持久化的持仓记录；新增 PositionsTab 展示持仓列表并触发复检；在 TrendScanTab 添加标记按钮；扩展 TrendScanResult 类型以暴露 currentPrice

**Tech Stack:** Vue 3, TypeScript, localStorage API, 现有的 useDataFetch/useTrendScore composables

## Global Constraints

- 不接交易所 API，不做自动下单
- 复检仅由用户显式触发（进入 Tab 或点按钮），不做后台定时轮询
- 持仓记录持久化在 localStorage，key 为 `trendscan_positions`
- 所有新类型定义遵循项目现有命名风格（接口名 PascalCase，字段名 camelCase）

---

### Task 1: 扩展类型定义以暴露 currentPrice

**Files:**
- Modify: `src/composables/useTrendScore.ts` (SwingSLTPResult 接口 约209-214行, calculateSwingSLTP 函数 约226-296行, scoreSymbol 函数 约411-463行)
- Modify: `src/types/index.ts` (TrendScanResult 接口 约164-193行)

**Interfaces:**
- Consumes: 无（第一个任务）
- Produces: `SwingSLTPResult.currentPrice: number`, `TrendScanResult.currentPrice: number`

- [ ] **Step 1: 在 SwingSLTPResult 接口中添加 currentPrice 字段**

```typescript
export interface SwingSLTPResult {
  stopLossTight: number
  stopLossWide: number
  takeProfit: number
  closeConfirmPrice: number
  riskRewardTight: number
  riskRewardWide: number
  isSwingBased: boolean
  trailingStopPercent: number
  currentPrice: number  // 新增
}
```

- [ ] **Step 2: 在 calculateSwingSLTP 返回值中包含 currentPrice**

```typescript
return {
  stopLossTight,
  stopLossWide,
  takeProfit,
  closeConfirmPrice,
  riskRewardTight: riskTight > 0 ? reward / riskTight : 0,
  riskRewardWide: riskWide > 0 ? reward / riskWide : 0,
  isSwingBased,
  trailingStopPercent,
  currentPrice  // 新增，变量在函数开头已定义
}
```

- [ ] **Step 3: 在 TrendScanResult 接口中添加 currentPrice 字段**

在 `src/types/index.ts` 的 `trailingStopPercent: number` 字段后添加：

```typescript
currentPrice: number  // 新增
```

- [ ] **Step 4: 在 scoreSymbol 返回值中包含 currentPrice**

在 `trailingStopPercent: sltp.trailingStopPercent,` 后添加：

```typescript
currentPrice: sltp.currentPrice,  // 新增
```

- [ ] **Step 5: 运行类型检查验证修改**

```bash
cd /d/work1/ok/freqtrade-webui && yarn build
```
预期：类型检查通过，无 TS 错误

- [ ] **Step 6: 提交**

```bash
cd /d/work1/ok && git add freqtrade-webui/src/composables/useTrendScore.ts freqtrade-webui/src/types/index.ts
git commit -m "feat: expose currentPrice in TrendScanResult for position tracking"
```

---

### Task 2: 创建 usePositions composable（数据结构与存储）

**Files:**
- Create: `src/composables/usePositions.ts`

**Interfaces:**
- Consumes: `TrendScanResult` from `src/types/index.ts`
- Produces: `Position`, `PositionReview` 接口；`usePositions()` 返回 `{ positions, addPosition, closePosition, loadPositions, savePositions }`

- [ ] **Step 1: 创建文件框架，定义类型**

```typescript
import { ref } from 'vue'
import type { TrendScanResult } from '../types'

const STORAGE_KEY = 'trendscan_positions'

export interface Position {
  id: string
  pair: string
  timeframe: string
  direction: 'long' | 'short'
  entryPrice: number
  entryTime: number
  entryTrendScore: number
  entryGridScore: number
  entryStrategyRecommendation: 'trend' | 'grid' | 'mixed' | 'avoid'
  entryStopLossWide: number
  status: 'open' | 'closed'
  closedTime?: number
  lastReview?: PositionReview
}

export interface PositionReview {
  reviewTime: number
  currentPrice: number
  currentTrendScore: number
  currentGridScore: number
  scoreDelta: number
  suggestion: 'hold' | 'reduce' | 'close'
  reasons: string[]
}

export function usePositions() {
  const positions = ref<Position[]>([])

  return {
    positions
  }
}
```

- [ ] **Step 2: 实现 loadPositions / savePositions，并加入返回对象**

```typescript
function loadPositions(): void {
  try {
    const stored = localStorage.getItem(STORAGE_KEY)
    if (stored) {
      positions.value = JSON.parse(stored)
    }
  } catch (e) {
    console.error('Failed to load positions from localStorage:', e)
    positions.value = []
  }
}

function savePositions(): void {
  try {
    localStorage.setItem(STORAGE_KEY, JSON.stringify(positions.value))
  } catch (e) {
    console.error('Failed to save positions to localStorage:', e)
  }
}
```

- [ ] **Step 3: 实现 addPosition**

```typescript
function addPosition(result: TrendScanResult): void {
  const id = `${result.pair}-${result.timeframe}-${Date.now()}`
  const newPosition: Position = {
    id,
    pair: result.pair,
    timeframe: result.timeframe,
    direction: result.direction,
    entryPrice: result.currentPrice,
    entryTime: Date.now(),
    entryTrendScore: result.trendScore,
    entryGridScore: result.gridScore,
    entryStrategyRecommendation: result.strategyRecommendation,
    entryStopLossWide: result.stopLossWide,
    status: 'open'
  }
  positions.value.push(newPosition)
  savePositions()
}
```

- [ ] **Step 4: 实现 closePosition，并整理最终返回对象**

```typescript
function closePosition(positionId: string): void {
  const position = positions.value.find(p => p.id === positionId)
  if (position) {
    position.status = 'closed'
    position.closedTime = Date.now()
    savePositions()
  }
}

return {
  positions,
  addPosition,
  closePosition,
  loadPositions,
  savePositions
}
```

- [ ] **Step 5: 运行类型检查**

```bash
cd /d/work1/ok/freqtrade-webui && yarn build
```
预期：类型检查通过

- [ ] **Step 6: 提交**

```bash
cd /d/work1/ok && git add freqtrade-webui/src/composables/usePositions.ts
git commit -m "feat: add usePositions composable with localStorage persistence"
```

---

### Task 3: 实现 evaluatePosition 复检规则引擎

**Files:**
- Modify: `src/composables/usePositions.ts`

**Interfaces:**
- Consumes: `Position`, `PositionReview`, `TrendScanResult`
- Produces: `evaluatePosition(position: Position, fresh: TrendScanResult): PositionReview`, `reviewPosition(positionId: string, review: PositionReview): void`

- [ ] **Step 1: 添加 evaluatePosition 函数（放在 closePosition 定义之后）**

```typescript
function evaluatePosition(position: Position, fresh: TrendScanResult): PositionReview {
  const scoreDelta = fresh.trendScore - position.entryTrendScore
  let suggestion: 'hold' | 'reduce' | 'close' = 'hold'
  const reasons: string[] = []

  // 规则1: 方向翻转
  if (fresh.direction !== 'neutral' && fresh.direction !== position.direction) {
    suggestion = 'close'
    reasons.push('趋势方向已反转')
  }

  // 规则2: 评分大幅走弱
  if (scoreDelta <= -30) {
    suggestion = 'close'
    reasons.push('趋势评分较建仓时下降超过30分')
  } else if (scoreDelta <= -15) {
    if (suggestion === 'hold') suggestion = 'reduce'
    reasons.push('趋势评分较建仓时下降超过15分')
  }

  // 规则3: 价格触及止损位
  const currentPrice = fresh.currentPrice
  const stopLossWide = position.entryStopLossWide
  const stopLossHit = position.direction === 'long'
    ? currentPrice <= stopLossWide
    : currentPrice >= stopLossWide

  if (stopLossHit) {
    suggestion = 'close'
    reasons.push('价格已触及建仓时的宽止损位')
  }

  // 无命中规则时给默认原因
  if (reasons.length === 0) {
    reasons.push('评分与建仓时相比无明显恶化')
  }

  return {
    reviewTime: Date.now(),
    currentPrice: fresh.currentPrice,
    currentTrendScore: fresh.trendScore,
    currentGridScore: fresh.gridScore,
    scoreDelta,
    suggestion,
    reasons
  }
}
```

- [ ] **Step 2: 添加 reviewPosition 函数**

```typescript
function reviewPosition(positionId: string, review: PositionReview): void {
  const position = positions.value.find(p => p.id === positionId)
  if (position) {
    position.lastReview = review
    savePositions()
  }
}
```

- [ ] **Step 3: 更新返回对象**

```typescript
return {
  positions,
  addPosition,
  closePosition,
  evaluatePosition,
  reviewPosition,
  loadPositions,
  savePositions
}
```

- [ ] **Step 4: 运行类型检查**

```bash
cd /d/work1/ok/freqtrade-webui && yarn build
```
预期：类型检查通过

- [ ] **Step 5: 提交**

```bash
cd /d/work1/ok && git add freqtrade-webui/src/composables/usePositions.ts
git commit -m "feat: implement evaluatePosition rule engine for position review"
```

---

### Task 4: 在 TrendScanTab 添加标记持仓按钮

**Files:**
- Modify: `src/tabs/TrendScanTab.vue` (script setup 部分约122-136行, 操作列约105-110行, style 部分)

**Interfaces:**
- Consumes: `usePositions().addPosition`, `TrendScanEntry`
- Produces: 表格操作列新增"标记持仓"按钮

- [ ] **Step 1: 引入 usePositions**

在 `<script setup>` 的 import 语句区域添加：

```typescript
import { usePositions } from '../composables/usePositions'

const { addPosition } = usePositions()
```

- [ ] **Step 2: 添加 handleMarkPosition 函数**

在 `emitViewKline` 函数后添加：

```typescript
function handleMarkPosition(result: TrendScanEntry) {
  if (!result.insufficientData) {
    addPosition(result)
  }
}
```

- [ ] **Step 3: 在操作列添加按钮**

将操作列的 `<td>` 修改为：

```vue
<td>
  <button class="btn btn-small btn-view" @click="emitViewKline(r)">
    查看K线
  </button>
  <button
    v-if="!r.insufficientData && r.direction !== 'neutral'"
    class="btn btn-small btn-mark"
    @click="handleMarkPosition(r)"
  >
    标记持仓
  </button>
</td>
```

- [ ] **Step 4: 添加按钮样式**

在 `.btn-view:hover` 规则后添加：

```css
.btn-mark { background: var(--accent-green); border-color: var(--accent-green); color: #fff; margin-left: 6px; }
.btn-mark:hover { background: #059669; border-color: #059669; }
```

- [ ] **Step 5: 手动验证**

```bash
cd /d/work1/ok/freqtrade-webui && yarn dev
```
在浏览器中运行一次趋势扫描，确认：
1. 每行（非中性方向、有数据）出现"标记持仓"按钮
2. 点击后浏览器 DevTools -> Application -> Local Storage 中出现 `trendscan_positions` 键，值包含刚标记的记录

- [ ] **Step 6: 提交**

```bash
cd /d/work1/ok && git add freqtrade-webui/src/tabs/TrendScanTab.vue
git commit -m "feat: add mark position button in trend scan table"
```

---

### Task 5: 创建 PositionsTab 组件

**Files:**
- Create: `src/tabs/PositionsTab.vue`

**Interfaces:**
- Consumes: `usePositions()`, `useDataFetch().loadData`, `scoreSymbol()` from `useTrendScore`, `Position`/`PositionReview` types
- Produces: `PositionsTab.vue`，展示持仓列表，支持挂载时自动复检、手动复检、平仓

- [ ] **Step 1: 创建模板与基础脚本骨架**

```vue
<template>
  <div class="tab-content">
    <div v-if="positions.length > 0" class="chart-card">
      <div class="chart-header">
        <div class="chart-title">💼 持仓管理</div>
        <div class="filter-buttons">
          <button
            v-for="filter in filters"
            :key="filter.value"
            class="btn btn-small"
            :class="{ active: currentFilter === filter.value }"
            @click="currentFilter = filter.value"
          >
            {{ filter.label }}
          </button>
        </div>
      </div>
      <div class="table-container">
        <table class="trades-table">
          <thead>
            <tr>
              <th>交易对</th>
              <th>周期</th>
              <th>方向</th>
              <th>建仓价</th>
              <th>建仓时间</th>
              <th>建仓分</th>
              <th>当前分</th>
              <th>分数变化</th>
              <th>建议</th>
              <th>命中原因</th>
              <th>操作</th>
            </tr>
          </thead>
          <tbody>
            <tr v-for="pos in filteredPositions" :key="pos.id">
              <td>{{ pos.pair }}</td>
              <td>{{ pos.timeframe }}</td>
              <td :class="pos.direction">{{ pos.direction === 'long' ? '做多' : '做空' }}</td>
              <td>{{ pos.entryPrice.toFixed(4) }}</td>
              <td>{{ formatTime(pos.entryTime) }}</td>
              <td>{{ pos.entryTrendScore }}</td>
              <td>{{ pos.lastReview ? pos.lastReview.currentTrendScore : '未复检' }}</td>
              <td v-if="pos.lastReview" :class="getScoreDeltaClass(pos.lastReview.scoreDelta)">
                {{ pos.lastReview.scoreDelta > 0 ? '+' : '' }}{{ pos.lastReview.scoreDelta }}
              </td>
              <td v-else>—</td>
              <td v-if="pos.lastReview" :class="getSuggestionClass(pos.lastReview.suggestion)">
                {{ getSuggestionLabel(pos.lastReview.suggestion) }}
              </td>
              <td v-else>—</td>
              <td>{{ pos.lastReview ? pos.lastReview.reasons.join('；') : '—' }}</td>
              <td>
                <button v-if="pos.status === 'open'" class="btn btn-small btn-review" @click="handleReview(pos)">
                  重新复检
                </button>
                <button v-if="pos.status === 'open'" class="btn btn-small btn-close" @click="handleClose(pos.id)">
                  平仓
                </button>
                <span v-if="pos.status === 'closed'" class="closed-badge">已平仓</span>
              </td>
            </tr>
          </tbody>
        </table>
      </div>
    </div>
    <div v-else class="empty-state">
      <p>暂无持仓记录，在趋势扫描页面标记后会显示在这里</p>
    </div>
  </div>
</template>

<script setup lang="ts">
import { ref, computed, onMounted } from 'vue'
import { usePositions } from '../composables/usePositions'
import { useDataFetch } from '../composables/useDataFetch'
import { scoreSymbol } from '../composables/useTrendScore'
import type { Position } from '../composables/usePositions'

const { positions, loadPositions, closePosition, evaluatePosition, reviewPosition } = usePositions()
const { loadData } = useDataFetch()

const currentFilter = ref('open')
const filters = [
  { label: '全部', value: 'all' },
  { label: '持仓中', value: 'open' },
  { label: '已平仓', value: 'closed' }
]

const filteredPositions = computed(() => {
  if (currentFilter.value === 'all') return positions.value
  return positions.value.filter(p => p.status === currentFilter.value)
})

function formatTime(timestamp: number): string {
  return new Date(timestamp).toLocaleString('zh-CN')
}

function getScoreDeltaClass(delta: number): string {
  if (delta > 0) return 'positive'
  if (delta < 0) return 'negative'
  return ''
}

function getSuggestionClass(suggestion: string): string {
  switch (suggestion) {
    case 'hold': return 'suggestion-hold'
    case 'reduce': return 'suggestion-reduce'
    case 'close': return 'suggestion-close'
    default: return ''
  }
}

function getSuggestionLabel(suggestion: string): string {
  switch (suggestion) {
    case 'hold': return '持有'
    case 'reduce': return '减仓'
    case 'close': return '平仓'
    default: return '未知'
  }
}

function handleClose(positionId: string): void {
  if (confirm('确定要平仓吗？')) {
    closePosition(positionId)
  }
}
</script>

<style scoped>
.tab-content { display: flex; flex-direction: column; gap: 24px; }
.chart-card { background: var(--bg-card); border-radius: 16px; padding: 24px; border: 1px solid var(--border-color); }
.chart-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 20px; flex-wrap: wrap; gap: 10px; }
.chart-title { font-size: 1.1rem; font-weight: 700; color: var(--text-primary); }
.filter-buttons { display: flex; gap: 8px; }
.btn { padding: 6px 12px; border: 1px solid var(--border-color); border-radius: 6px; font-size: 0.8rem; cursor: pointer; background: var(--bg-secondary); color: var(--text-primary); transition: all 0.3s; }
.btn:hover, .btn.active { background: var(--accent-blue); border-color: var(--accent-blue); color: #fff; }
.table-container { overflow: auto; max-height: 500px; }
.trades-table { width: 100%; border-collapse: collapse; }
.trades-table th, .trades-table td { padding: 10px 12px; text-align: left; border-bottom: 1px solid var(--border-color); font-family: 'Space Mono', monospace; font-size: 0.8rem; }
.trades-table th { color: var(--text-secondary); font-weight: 400; text-transform: uppercase; letter-spacing: 1px; font-size: 0.7rem; position: sticky; top: 0; background: var(--bg-card); }
.trades-table tr:hover { background: rgba(59, 130, 246, 0.05); }
.long { color: var(--accent-green); }
.short { color: var(--accent-red); }
.positive { color: var(--accent-green); font-weight: bold; }
.negative { color: var(--accent-red); font-weight: bold; }
.suggestion-hold { color: var(--accent-green); font-weight: bold; }
.suggestion-reduce { color: var(--accent-gold); font-weight: bold; }
.suggestion-close { color: var(--accent-red); font-weight: bold; }
.btn-review { background: var(--accent-blue); border-color: var(--accent-blue); color: #fff; margin-right: 6px; }
.btn-review:hover { background: #2563eb; border-color: #2563eb; }
.btn-close { background: var(--accent-red); border-color: var(--accent-red); color: #fff; }
.btn-close:hover { background: #dc2626; border-color: #dc2626; }
.closed-badge { color: var(--text-secondary); font-style: italic; }
.empty-state { text-align: center; padding: 60px 20px; color: var(--text-secondary); }
</style>
```

- [ ] **Step 2: 实现 handleReview（单条手动复检）**

在 `handleClose` 函数后添加：

```typescript
async function handleReview(position: Position): Promise<void> {
  try {
    const candle = await loadData(position.pair, position.timeframe, 500)
    if (candle.data.length < 100) {
      alert('数据不足，无法复检')
      return
    }
    const fresh = scoreSymbol(position.pair, position.timeframe, candle.data, true)
    if (fresh.insufficientData) {
      alert('数据不足，无法复检')
      return
    }
    const review = evaluatePosition(position, fresh)
    reviewPosition(position.id, review)
  } catch (e) {
    console.error('复检失败:', e)
    alert('复检失败，请重试')
  }
}
```

- [ ] **Step 3: 实现 reviewAllOpenPositions 并在 onMounted 中调用**

在 `<script setup>` 顶部变量声明区之后、`handleReview` 之后添加：

```typescript
async function reviewAllOpenPositions(): Promise<void> {
  const openPositions = positions.value.filter(p => p.status === 'open')
  for (const position of openPositions) {
    try {
      const candle = await loadData(position.pair, position.timeframe, 500)
      if (candle.data.length < 100) continue
      const fresh = scoreSymbol(position.pair, position.timeframe, candle.data, true)
      if (fresh.insufficientData) continue
      const review = evaluatePosition(position, fresh)
      reviewPosition(position.id, review)
    } catch (e) {
      console.error(`复检 ${position.pair} ${position.timeframe} 失败:`, e)
    }
  }
}

onMounted(() => {
  loadPositions()
  reviewAllOpenPositions()
})
```

（注：`onMounted` 需已在 import 中包含，Step 1 骨架已包含）

- [ ] **Step 4: 运行类型检查**

```bash
cd /d/work1/ok/freqtrade-webui && yarn build
```
预期：类型检查通过

- [ ] **Step 5: 提交**

```bash
cd /d/work1/ok && git add freqtrade-webui/src/tabs/PositionsTab.vue
git commit -m "feat: create PositionsTab with review and close functionality"
```

---

### Task 6: 在 App.vue 中注册"持仓"Tab

**Files:**
- Modify: `src/App.vue` (import 区域约第88行, tabs 数组约第92-98行, template 中 display-area 约第44-71行)

**Interfaces:**
- Consumes: `PositionsTab` from `src/tabs/PositionsTab.vue`
- Produces: 左侧导航新增"持仓"Tab，可切换查看

- [ ] **Step 1: 引入 PositionsTab 组件**

在 `src/App.vue` 的 `<script setup>` 中，`import TrendScanTab from './tabs/TrendScanTab.vue'` 后添加：

```typescript
import PositionsTab from './tabs/PositionsTab.vue'
```

- [ ] **Step 2: 在 tabs 数组中添加"持仓"项**

修改 `tabs` 数组：

```typescript
const tabs = [
  { name: 'backtest', label: '回测结果', icon: '📈' },
  { name: 'optimize', label: '参数优化', icon: '🔍' },
  { name: 'scan', label: '多品种扫描', icon: '📊' },
  { name: 'trendscan', label: '趋势扫描', icon: '📈' },
  { name: 'positions', label: '持仓', icon: '💼' },
  { name: 'validate', label: '滑动窗口验证', icon: '✅' }
]
```

- [ ] **Step 3: 在 display-area 中挂载 PositionsTab**

在 `<TrendScanTab ... />` 组件标签后、`<ValidateTab ... />` 前添加：

```vue
<PositionsTab v-show="activeTab === 'positions'" />
```

- [ ] **Step 4: 运行类型检查**

```bash
cd /d/work1/ok/freqtrade-webui && yarn build
```
预期：类型检查通过

- [ ] **Step 5: 手动端到端验证**

```bash
cd /d/work1/ok/freqtrade-webui && yarn dev
```
在浏览器中：
1. 运行一次趋势扫描
2. 对某一行点击"标记持仓"
3. 切换到左侧"持仓"Tab，确认该记录出现，且挂载后自动完成了一次复检（当前分不再显示"未复检"）
4. 点击"重新复检"，确认可再次刷新
5. 点击"平仓"，确认状态变为"已平仓"，切换筛选按钮可看到该记录
6. 刷新整个页面，确认持仓记录仍然存在（localStorage 持久化生效）

- [ ] **Step 6: 提交**

```bash
cd /d/work1/ok && git add freqtrade-webui/src/App.vue
git commit -m "feat: register positions tab in App.vue"
```

---

## Self-Review Notes

- **Spec 覆盖检查**：第一节 Position 数据结构 → Task 2；第二节 evaluatePosition 规则引擎 → Task 3；第三节 数据获取与复检触发（挂载时+手动按钮）→ Task 5；第四节 标记持仓按钮 + currentPrice 缺口 → Task 1 + Task 4；第五节 PositionsTab 表格设计与平仓/历史行为 → Task 5。全部覆盖。
- **占位符扫描**：全部步骤均含完整代码，无 TBD/TODO。
- **类型一致性**：`Position`/`PositionReview` 字段名在 Task 2、3、5 中保持一致；`usePositions()` 返回的函数名（`addPosition`/`closePosition`/`evaluatePosition`/`reviewPosition`/`loadPositions`/`savePositions`）在各任务间一致引用。
